import { Helmet } from 'react-helmet'

const NotFoundPage = () => (
  <div>
    <Helmet>
      <title>Not found | 5G-V2X</title>
    </Helmet>
    Not found page
  </div>
)

export default NotFoundPage
